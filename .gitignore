node_modules/
dist/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# secrets
.env
.env.*
# 템플릿은 예외 — 비밀값이 없고, 필수 변수의 형식을 알려주는 유일한 추적 파일이다
!.env.example

CLAUDE.md

# Supabase CLI 로컬 상태 — 프로젝트 ref·org id·pooler URL 이 들어 있다
supabase/.temp/

# 실제 인프라 식별자 — 공개 레포에 올리지 않는다
docs/.local/

# 이관 페이로드 — 레거시 본문과 Supabase 이미지 URL 이 들어 있다. 공개 레포에 올리지 않는다
docs/legacy-dump/posts-payload.json

# RDS CA 번들 — 공개 인증서지만 리전별로 갱신되므로 스크립트로 받는다
certs/
.vercel
.env*
